# tool-specific-tips — create-interest

No special tooling. Markdown in, markdown out.

- If the user wants an image, save to `static/images/interests/<slug>.jpg` and inline with `![alt](/images/interests/<slug>.jpg)` in the body.
- For external references, verify URLs are live before adding (a broken link in a short page is very visible). Use whatever fetch tool your assistant has.
